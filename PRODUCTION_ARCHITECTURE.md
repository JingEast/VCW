# 高可用部署架构

## 部署拓扑

```
                         Internet
                            │
                    ┌───────┴───────┐
                    │   Cloudflare   │
                    │   (DNS + WAF)  │
                    └───────┬───────┘
                            │
                    ┌───────┴───────┐
                    │     Nginx      │
                    │  (SSL + LB)    │
                    └───────┬───────┘
                            │
            ┌───────────────┼───────────────┐
            │               │               │
      ┌─────┴─────┐   ┌─────┴─────┐   ┌─────┴─────┐
      │ Gunicorn  │   │ Gunicorn  │   │ Gunicorn  │
      │  Worker   │   │  Worker   │   │  Worker   │
      │  (Flask)  │   │  (Flask)  │   │  (Flask)  │
      └─────┬─────┘   └─────┬─────┘   └─────┬─────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
                    ┌───────┴───────┐
                    │   PostgreSQL   │
                    │  (Primary +    │
                    │   Replica)     │
                    └───────┬───────┘
                            │
                    ┌───────┴───────┐
                    │     Redis      │
                    │  (Sentinel)    │
                    └───────────────┘
```

## 服务规格

| 服务 | 实例数 | CPU | 内存 | 说明 |
|------|--------|-----|------|------|
| Nginx | 1 | 1核 | 512M | 反向代理 + 静态文件 |
| Gunicorn | 3 | 2核 | 1G | 4 workers / 实例 |
| PostgreSQL | 2 | 2核 | 2G | Primary + Replica |
| Redis | 3 | 1核 | 1G | Sentinel 高可用 |
| Celery Worker | 2 | 2核 | 2G | 4 concurrency |
| Celery Beat | 1 | 1核 | 512M | 定时任务调度 |

## 容灾方案

| 层级 | 方案 | RTO | RPO |
|------|------|-----|-----|
| 应用层 | 多实例 + Nginx 健康检查 | 1min | 0 |
| 数据库 | PostgreSQL 流复制 + 自动故障转移 | 5min | < 1min |
| 缓存 | Redis Sentinel 自动主从切换 | 30s | 0 |
| 数据备份 | 每日全量 + WAL 归档 | - | 24h |

## 扩容方案

| 触发条件 | 扩容动作 |
|----------|----------|
| CPU > 70% | Gunicorn +1 |
| 内存 > 80% | Gunicorn +1 |
| DB 连接 > 80% | 连接池扩容 |
| 队列堆积 > 100 | Worker +1 |

## 备份方案

| 数据 | 频率 | 保留期 | 存储 |
|------|------|--------|------|
| PostgreSQL | 每日全量 | 30天 | S3 |
| Redis RDB | 每小时 | 7天 | S3 |
| 应用日志 | 实时 | 90天 | S3 |
| 生成的文案 | 实时 | 永久 | S3 |
